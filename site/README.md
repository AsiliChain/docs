# AsiliChain Docs Site

Documentation and whitepaper for the AsiliChain Protocol.

## Development

```bash
npm install
npm start
```

## Build

```bash
npm run build
npm run serve
```

Deployment to GitHub Pages is automated via GitHub Actions.


For automated GitHub Pages publishing, keep the Docusaurus `deploy` command and point it at the repository branch that hosts Pages content.
