---
import AiCtaButtons from './AiCtaButtons.astro';
---
<AiCtaButtons />
{/*
  Note: Pagination is handled automatically by Starlight's default.
  Custom prev/next requires using Starlight's sidebar config instead of component override.
*/}
<style>
  /* Hide debug */
  .ac-debug { display: none; }
</style>